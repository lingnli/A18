<h3 class="text-center mt-3">請修改你的支出</h3>
<h6 class="text-center mt-4">*欄位為必填</h6>

<form method="POST" action="/record/edit/{{records.id}}?_method=PUT" class="form-new mx-auto mt-3" style="width:480px">

  <div class="form-group row  form-group-new">
    <label for="name" class="col-form-label mt-2">*名稱：
    </label>
    <div class="col-sm-10">
      <input type="text" class="form-control input-new" name="name" id="name" value="{{records.name}}" required>
    </div>
  </div>

  <div class="form-group row form-group-new">
    <label for="date" class="col-form-label mt-2">*日期：</label>
    <div class="col-sm-10">
      <input type="date" class=" form-control input-new" name="date" id="date" value="{{formatDate}}" required>
    </div>
  </div>

  <div class="form-group row form-group-new">
    <label for="category" class="col-form-label mt-2">*類別：</label>
    <select class="select-new" style="display:inline;" id="category" name="category" required>
      <option value="{{records.category}}">{{records.category}}</option>
      <option value="家居物業">家居物業</option>
      <option value="交通出行">交通出行</option>
      <option value="休閒娛樂">休閒娛樂</option>
      <option value="餐飲食品">餐飲食品</option>
      <option value="其他">其他</option>
    </select>
  </div>

  <div class="form-group row form-group-new">
    <label for="merchant" class="col-form-label mt-2 ml-2"> 店家： </label>
    <div class="col-sm-10">
      <input type="text" class="form-control input-new" name="merchant" id="merchant" value="{{records.merchant}}">
    </div>
  </div>

  <div class="form-group row form-group-new">
    <label for="amount" class="col-form-label mt-2">*金額：</label>
    <div class="col-sm-10">
      <input type="number" class="form-control input-new" name="amount" id="amount" value="{{records.amount}}" required>
    </div>
  </div>


  <div class="mx-auto" style="width: 200px;">
    <button type="submit" class="btn-new btn btn-outline-dark mb-3" style="width:200px;">確定修改支出</button>
  </div>

</form>